# 项目规则

任务涉及 IoT 共同架构、平台契约的跨仓联动，或需要访问第二个仓库时，先读取 [`iot-base` 跨仓规则](../../platform/iot-base/AGENTS.md) 和相关权威文档，明确目标仓及授权范围；访问或修改每个目标仓前仍须读取该仓自己的规则。

## IoT高效推进与验证原则

- 按当前合理方案直接实施，零历史兼容、零数据迁移；不为旧测试数据保留兼容层、迁移工具或双轨实现，不扩大到凭据和无关数据清除。
- 开发交付以代码评审及本次改动相关的快速、定向检查为准：覆盖关键成功路径、明显故障与必要安全边界；通过且无阻塞问题后立即推进，不反复规划或追加无关测试。
- 长稳、soak、长时间压测、物理满容量及反复断电实验由用户在自己搭建的半生产、试运行环境中逐步验证并反馈。代理不主动执行、不等待实验窗口，也不把这些列为编码、提交或当前开发交付的前置条件；仅在用户明确要求专项实验时执行。
- 已通过且未受影响的检查直接复用；只在新改动、明确失败或证据疑点影响当前结论时重验。不得默认扩展全量/多芯片/多Core矩阵、反复重建工具链或占用机器长时间运行；纯规则/文档修改只检查内容、链接与diff。
- 效率不等于削弱TLS、重连、WDT、OTA、CRC、写入完整性或执行器安全；已知缺陷必须修复或明确停下说明。未做的运行验证如实标注，不冒称通过。本文及所引用文档中的旧验证要求若与本节冲突，以本节为准；正式发布与物理操作仍按各自授权边界执行。

> 目录于 2026-09-08 从 `/Users/tyg/dir/codex_dir/Stc8hIrLamp` 迁移到 `/Users/tyg/workspace/iot/devices/Stc8hIrLamp`；仓库仍可能包含旧路径，执行相关命令前先按新目录核对并修正，完成本项目的路径清理和验证后删除本条。

- 本仓库是 STC8H 红外遥控夜灯产品仓库，包含夜灯固件、遥控器固件和老 Keil 工程归档。
- `firmware/lamp` 和 `firmware/remote` 使用相邻目录中的 `Stc8hBase` 基础库，不复制基础库源码。
- `legacy/` 为历史归档，除非用户明确要求，不修改老 Keil 工程内容。
- 新固件构建产物不提交：`firmware/lamp/build/`、`firmware/remote/.pio/`。
- 修改固件后必须分别构建对应项目，并说明是否需要硬件复测。
- 依赖 `Stc8hBase` 的版本变更必须记录到 `docs/dependency.md`。
- 不为旧 Keil 工程的不合理设计增加兼容层；新实现以当前硬件和验收行为为准。
- 本文件保持在 100 行以内。
